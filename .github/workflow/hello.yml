---
name: "hello to main branch"
on:
  push:
    branches:
      - developer
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello to main branch"
        run: echo "Hello to main branch"
